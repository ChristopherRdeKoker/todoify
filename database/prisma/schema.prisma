generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("POSTGRES_URL")
}

model account_user {
  id       Int    @id
  name     String @db.VarChar(255)
  email    String @unique
  password String
}
